# Web framework
flask==3.1.0

# ML — model & tokenizer (T5ForConditionalGeneration, T5Tokenizer)
transformers==4.51.3
torch==2.5.1
sentencepiece==0.2.0        # required by T5Tokenizer

# ML — training only (used in model/train.py)
datasets==3.5.0             # HuggingFace dataset loader
accelerate==1.3.0           # faster training on GPU/MPS
numpy==2.2.4